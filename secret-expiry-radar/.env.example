# Generate a token: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Paste it below before starting Docker. Never commit .env.
RADAR_API_TOKEN=
RADAR_DB=/data/radar.db
SLACK_WEBHOOK_URL=
PAGERDUTY_ROUTING_KEY=
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM=
SMTP_TO=
SMTP_USER=
SMTP_PASSWORD=
